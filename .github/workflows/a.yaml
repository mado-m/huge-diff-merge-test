name: a dir pull request path test

on:
  push:
    paths:
      - _EDIT_TO_BUILD
      - a/**
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - run: echo "This is test"

